import { Header } from "./components/Header";
import { Router } from "./Router";

export const Main = () => {
  return (
    <div className="Main">
      <Header />
      <Router />
    </div>
  );
};
